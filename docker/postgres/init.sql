-- Initialize database with proper extensions
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Create database if it doesn't exist (handled by PostgreSQL image)
-- This file is for any additional database setup
